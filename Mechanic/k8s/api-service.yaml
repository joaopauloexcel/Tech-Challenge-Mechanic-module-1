apiVersion: v1
kind: Service
metadata:
  name: mechanic-api-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: mechanic-api
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30007